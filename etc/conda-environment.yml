name: anaconda-client-env
channels:
  - conda-forge
  - pytorch
  - defaults
dependencies:
  - anaconda-client
  - torch=2.0.0
  - torchvision
  - torchaudio
  - pip
  - pip:
      - -r requirements.txt
